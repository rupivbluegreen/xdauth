## What and why

## Checklist

- [ ] `go build ./...`, `go vet ./...`, `gofmt -l .` are clean
- [ ] `go test ./...` passes
- [ ] If this touches `internal/broker`, added/updated a phishing test (see `docs/threat-model.md`)
- [ ] Commit messages are Conventional Commits, one line each
